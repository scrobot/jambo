spring:
  application:
    name: auth
  cloud:
    consul:
      config:
        enabled: false
        prefix: jambo/sso
        format: yaml
      host: ${CONSUL_URL:http://consul}
      port: ${CONSUL_PORT:8500}
